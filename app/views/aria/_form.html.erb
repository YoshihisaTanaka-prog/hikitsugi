<%= form_with(model: arium) do |form| %>
  <% if arium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(arium.errors.count, "error") %> prohibited this arium from being saved:</h2>

      <ul>
        <% arium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
